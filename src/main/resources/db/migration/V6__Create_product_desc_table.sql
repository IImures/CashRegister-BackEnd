create table product_description (
                                     id bigint not null,
                                     description varchar(255) not null,
                                     primary key (id)
);